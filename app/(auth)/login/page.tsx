/**
 * Login page — placeholder for Phase 2.
 * The full email/password form + Google button will be built in Phase 2.
 */
export default function LoginPage() {
  return (
    <div className="flex min-h-dvh items-center justify-center bg-cream px-4">
      <div className="w-full max-w-sm rounded-2xl bg-parchment p-8 shadow-card text-center">
        <h1 className="font-serif text-2xl font-semibold text-ink">Sign in</h1>
        <p className="mt-2 text-sm text-muted">Full login UI coming in Phase 2.</p>
      </div>
    </div>
  );
}

export const metadata = {
  title: "Sign In",
};
